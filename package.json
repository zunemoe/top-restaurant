{
  "name": "restaurant-page",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start:localhost": "npx webpack serve --env localhost",
    "start:network": "npx webpack serve --env network",
    "build:dev": "npx webpack --mode=development",
    "build:prod": "npx webpack --env production --mode=production",
    "deploy": "npm run build && npx gh-pages -d dist -o origin",
    "deploy:clean": "npm run build:prod && npx gh-pages -d dist -f -o origin"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "css-loader": "^7.1.2",
    "html-loader": "^5.1.0",
    "html-webpack-plugin": "^5.6.3",
    "style-loader": "^4.0.0",
    "webpack": "^5.100.1",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.2.2"
  }
}
